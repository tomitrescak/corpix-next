import "mocha-jest";
import "@testing-library/jest-dom/extend-expect";
// import { cleanup } from "@testing-library/react";

// if (!global._mocked) {
//   mock.useFakeTimers();
//   global._mocked = true;
// }

// afterEach(cleanup);
